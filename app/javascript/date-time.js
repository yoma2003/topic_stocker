// const dateTime = () => {
//   const flatpickrs = document.querySelectorAll(".flatpickr");
//   flatpickrs.forEach((f) => {
//     flatpickr(f);
//   });
// };

// window.addEventListener("load", dateTime);

const dateTime = () => {
  flatpickr("#flatp");
};

window.addEventListener("load", dateTime);